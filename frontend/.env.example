# Base URL of the FastAPI backend's v1 API.
# Relative on purpose: the Vite dev server proxies /api/* to the backend (see
# vite.config.js) so the browser sees the frontend and API as the same origin —
# required for the httpOnly auth cookie to work without HTTPS in local dev.
VITE_API_URL=/api/v1
